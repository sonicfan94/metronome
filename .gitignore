# OS / editor junk
.DS_Store
*.swp

# Local share artifact (not needed in the repo)
scale-trainer.zip

# Node / Capacitor
node_modules/
# CocoaPods (regenerated by `pod install` / `cap sync`)
ios/App/Pods/
ios/App/Podfile.lock
ios/App/App/public/
