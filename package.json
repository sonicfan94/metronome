{
  "name": "scale-trainer",
  "version": "2.0.0",
  "description": "Scale Trainer — a Y2K skeuomorphic metronome built for practicing guitar scales.",
  "private": true,
  "scripts": {
    "serve": "python3 -m http.server --directory www 8080",
    "sync": "cap sync",
    "ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/haptics": "^6.0.2",
    "@capacitor/preferences": "^6.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  }
}
